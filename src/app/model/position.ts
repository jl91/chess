import {PositionsEnum} from '../enum/positions.enum';

export class Position {
  coordinate: PositionsEnum;
  dstX: number;
  dstY: number;
  startX: number;
  endX: number;
  startY: number;
  endY: number;
}
